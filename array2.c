//Program to take 5 values from the user and store them in array
// print the element stored int the array
#include<stdio.h>
int main()
{
	int values[5],i;
	printf("Enter 5 integers:");
	scanf("%d", &values[i]);
	for(i=0;i<5; ++i)
	{
	printf("displaying integers:");
	}
	for(i= 0; i<5; ++i)
	{
	printf("%d\n",values[i]);
	}
	return(0);
	}

#include<stdio.h>
int main()
{
int base, height, area;
printf("Enter the base :");
scanf("%d",&base);
printf("Enter the height:");
scanf("%d",&height);
area = (base*height)/2;
printf("area of Tringle %d ",area);
return(0);
}
